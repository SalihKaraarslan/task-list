# Address of the GraphQL backend, as seen from your phone.
# Leave it empty (or do not create a .env file) to use the computer that runs "expo start".
# Set it only when that does not work, for example: http://192.168.1.20:4000/graphql
EXPO_PUBLIC_API_URL=
